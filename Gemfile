source "https://rubygems.org"

# Matches what GitHub Pages runs, so local preview and production agree.
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
  gem "jekyll-sitemap"
end

gem "webrick"
